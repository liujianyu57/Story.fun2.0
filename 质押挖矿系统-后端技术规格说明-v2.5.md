# Story.fun 质押挖矿系统 — 后端开发技术规格说明 v2.5

> **基于《Story.fun 代币经济模型 v2.5》提取的质押挖矿及相关参数计算，面向后端开发工程师。**
>
> 版本：v2.5 · 2026-06-09

---

## 目录

1. [概述](#1-概述)
2. [核心数据模型](#2-核心数据模型)
3. [Heat 计算子系统（每日批处理）](#3-heat-计算子系统每日批处理)
4. [咖位体系与升级合成](#4-咖位体系与升级合成)
5. [体力子系统](#5-体力子系统)
6. [质押挖矿与周结算](#6-质押挖矿与周结算)
7. [邀请挖矿](#7-邀请挖矿)
8. [STORY 释放曲线](#8-story-释放曲线)
9. [核心参数速查表](#9-核心参数速查表)
10. [风控与边界条件](#10-风控与边界条件)

---

## 1. 概述

### 1.1 系统定位

质押挖矿是 Story.fun 经济模型的核心——用户质押演员 NFT，通过演员绑定的短剧产生的真实数据（Heat），按咖位系数和有效工作时长获得 STORY 奖励。

**飞轮逻辑：**

```
创作者制作短剧 → 用户免费刷剧 → 数据沉淀到演员 ID → Heat↑
→ 持卡人质押挖矿收益↑ → 资产预期↑ → 激励更多创作与交易
```

**关键原则：**
- 没有数据的演员 NFT → Heat=0 → 挖矿产出为 0
- 没有绑定演员的短剧 → 不进入演员经济闭环
- 每周有硬顶约束，防超发

### 1.2 角色与职责

| 角色 | 挖矿相关行为 |
|---|---|
| **持卡人** | 持有、质押、升级、刷新体力、获得 STORY 挖矿收益 |
| **创作者/发行者** | 创建演员 NFT、绑定剧、获得 Mint/版税收入（40%份额） |
| **邀请人** | 从邀请子池按实得 STORY 获得额外奖励 |
| **平台** | 中心化数据计算、反作弊、结算、运营参数调整 |

---

## 2. 核心数据模型

> 以下为建议的表结构设计，供后端研发参考。链上（NFT、STORY）与中心化（Heat、体力、收益）分离。

### 2.1 演员体系

```sql
-- 演员 ID 表（中心化）
CREATE TABLE actor (
    actor_id        BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    name            VARCHAR(100) NOT NULL,
    creator_address VARCHAR(42) NOT NULL,      -- 发行者钱包
    p0              DECIMAL(18,6) NOT NULL,     -- 初始 Mint 价格 (USDC)
    total_supply    INT UNSIGNED NOT NULL,      -- 总发行量
    minted_count    INT UNSIGNED NOT NULL DEFAULT 0,
    vault_balance   DECIMAL(18,6) NOT NULL DEFAULT 0, -- Vault USDC
    created_at      DATETIME NOT NULL
);

-- 演员 NFT 实例表（数据中心化同步链上状态）
CREATE TABLE actor_nft (
    token_id        BIGINT UNSIGNED NOT NULL,
    actor_id        BIGINT UNSIGNED NOT NULL,
    level           TINYINT UNSIGNED NOT NULL DEFAULT 1,  -- 1=Lv1 ~ 5=Lv5
    owner_address   VARCHAR(42) NOT NULL,
    user_id         BIGINT UNSIGNED,             -- 平台用户 ID（中心化账号）
    stamina         INT UNSIGNED NOT NULL DEFAULT 10080, -- 体力（分钟）
    is_staking      TINYINT(1) NOT NULL DEFAULT 0,
    trust           DECIMAL(4,2) NOT NULL DEFAULT 1.00,
    created_at      DATETIME NOT NULL,
    updated_at      DATETIME NOT NULL,
    PRIMARY KEY (actor_id, token_id),
    INDEX idx_owner (owner_address),
    INDEX idx_user_staking (user_id, is_staking)
);
```

### 2.2 短剧与绑定

```sql
-- 剧表（中心化）
CREATE TABLE drama (
    drama_id        BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    title           VARCHAR(200) NOT NULL,
    creator_address VARCHAR(42) NOT NULL,
    publish_time    DATETIME NOT NULL,
    is_banned       TINYINT(1) NOT NULL DEFAULT 0, -- 风控下架标志
    created_at      DATETIME NOT NULL
);

-- 演员-剧绑定表
CREATE TABLE actor_drama_binding (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    actor_id        BIGINT UNSIGNED NOT NULL,
    drama_id        BIGINT UNSIGNED NOT NULL,
    binder_address  VARCHAR(42) NOT NULL,       -- 绑定时需持有至少 1 张 NFT
    bind_time       DATETIME NOT NULL,
    is_active       TINYINT(1) NOT NULL DEFAULT 1, -- 风控置为 0 则失效
    UNIQUE KEY uk_drama_actor (drama_id, actor_id),
    INDEX idx_actor (actor_id)
);
-- 约束：单剧最多 5 个演员 ID
-- 约束：bind_time 必须在 publish_time + 7×24h 内
-- 绑定后默认永久锁定，仅风控可置为 inactive
```

### 2.3 数据与 Heat

```sql
-- 剧日聚合指标表
CREATE TABLE drama_daily_metrics (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    drama_id        BIGINT UNSIGNED NOT NULL,
    stat_date       DATE NOT NULL,
    valid_play      INT UNSIGNED NOT NULL DEFAULT 0,
    valid_complete  INT UNSIGNED NOT NULL DEFAULT 0,
    like_count      INT UNSIGNED NOT NULL DEFAULT 0,
    favorite_count  INT UNSIGNED NOT NULL DEFAULT 0,
    comment_count   INT UNSIGNED NOT NULL DEFAULT 0,
    data_version    INT UNSIGNED NOT NULL DEFAULT 1,  -- 权重版本
    UNIQUE KEY uk_drama_date (drama_id, stat_date)
);

-- 演员日 Heat 表
CREATE TABLE actor_heat_daily (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    actor_id        BIGINT UNSIGNED NOT NULL,
    stat_date       DATE NOT NULL,
    raw_score       DECIMAL(24,4) NOT NULL DEFAULT 0,
    heat            DECIMAL(8,4) NOT NULL DEFAULT 0, -- 最终 Heat 值
    normalizer      DECIMAL(24,4) NOT NULL,          -- 当日归一化基准值
    UNIQUE KEY uk_actor_date (actor_id, stat_date)
);
```

### 2.4 质押与收益

```sql
-- 质押记录表
CREATE TABLE stake_record (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    user_id         BIGINT UNSIGNED NOT NULL,
    actor_id        BIGINT UNSIGNED NOT NULL,
    token_id        BIGINT UNSIGNED NOT NULL,
    stake_time      DATETIME NOT NULL,
    unstake_time    DATETIME,               -- NULL 表示仍在质押
    is_active       TINYINT(1) NOT NULL DEFAULT 1,
    INDEX idx_user (user_id, is_active),
    INDEX idx_actor_token (actor_id, token_id)
);
-- 约束：每个用户最多 5 条 active 质押记录

-- 体力变更流水表
CREATE TABLE stamina_log (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    actor_id        BIGINT UNSIGNED NOT NULL,
    token_id        BIGINT UNSIGNED NOT NULL,
    change_amount   INT NOT NULL,           -- 正=增加，负=消耗
    balance_after   INT UNSIGNED NOT NULL,
    change_type     ENUM('CONSUME', 'REST', 'RECHARGE', 'RESET'),
    created_at      DATETIME NOT NULL
);

-- 周收益结算表
CREATE TABLE weekly_reward (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    user_id         BIGINT UNSIGNED NOT NULL,
    week_start      DATE NOT NULL,          -- 结算周期起始（UTC 周一）
    nominal_reward  DECIMAL(24,6) NOT NULL, -- 名义产出
    scaled_reward   DECIMAL(24,6) NOT NULL, -- 经周硬顶缩放后
    actual_reward   DECIMAL(24,6) NOT NULL, -- 再经单地址封顶后
    claimed         TINYINT(1) NOT NULL DEFAULT 0,
    claim_tx_hash   VARCHAR(66),
    created_at      DATETIME NOT NULL,
    INDEX idx_user_week (user_id, week_start)
);
```

### 2.5 邀请体系

```sql
-- 邀请关系表
CREATE TABLE referral (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    inviter_id      BIGINT UNSIGNED NOT NULL,
    invitee_id      BIGINT UNSIGNED NOT NULL,
    level           TINYINT UNSIGNED NOT NULL, -- 1~5 级
    created_at      DATETIME NOT NULL,
    UNIQUE KEY uk_inviter_invitee (inviter_id, invitee_id)
);

-- 邀请周收益表
CREATE TABLE referral_weekly_reward (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    inviter_id      BIGINT UNSIGNED NOT NULL,
    week_start      DATE NOT NULL,
    nominal_reward  DECIMAL(24,6) NOT NULL,
    actual_reward   DECIMAL(24,6) NOT NULL,
    claimed         TINYINT(1) NOT NULL DEFAULT 0,
    INDEX idx_inviter_week (inviter_id, week_start)
);
```

---

## 3. Heat 计算子系统（每日批处理）

### 3.1 数据来源与有效定义

Heat 使用以下五类数据，均先经反作弊过滤：

| 数据项 | 权重 | 说明 |
|---|---|---|
| `valid_play` | × 1.0 | 有效播放 |
| `valid_complete` | × 5.0 | 有效完播（观看进度 ≥ 90%） |
| `like` | × 2.0 | 点赞 |
| `favorite` | × 3.0 | 收藏 |
| `comment` | × 4.0 | 评论 |

**累计完播**（用于升级门槛和发行人解锁）只使用 `valid_complete`，不使用其他数据。

### 3.2 单剧日热度分

```sql
drama_day_score =
    valid_play     × 1.0
  + valid_complete × 5.0
  + like_count     × 2.0
  + favorite_count × 3.0
  + comment_count  × 4.0
```

### 3.3 剧到演员映射

- 一部剧最多绑定 5 个演员 ID
- 多演员不拆分数据——同一部剧的 `drama_day_score` 同时完整计入每个绑定演员
- 7×24h 窗口内绑定 → 数据从剧发布时间追溯
- 绑定关系默认永久锁定，仅风控可置为 `inactive`

**映射逻辑伪代码：**

```
for each drama_id with drama_day_score > 0:
    for each binding in actor_drama_binding WHERE drama_id AND is_active=1:
        actor_raw_score_by_drama[actor_id] += drama_day_score
```

### 3.4 演员近期原始分（衰减聚合）

使用最近 30 天数据，按时间指数衰减：

```
**# stat_date = 当前正在计算的日期（批处理 T-1 日）**
**# data_date = 数据实际发生的日期**
days_ago = stat_date - data_date
weight_decay(days_ago) = 0.5 ^ (days_ago / HALF_LIFE_DAYS)

actor_raw_score(stat_date) = Σ drama_day_score × weight_decay

# HALF_LIFE_DAYS 为中心化参数（建议默认 7 天）
# LOOKBACK_DAYS = 30
# 求和范围：data_date 在最近 LOOKBACK_DAYS 内，且与 actor_id 存在 active 绑定关系
**# effective_from = 绑定关系的生效时间**
**#   - 7 天窗口内完成绑定的 → effective_from = 剧的发布时间（数据追溯起点）**
**#   - 正常绑定的 → effective_from = bind_time**
**# data_date >= effective_from 确保绑定前的数据不计入**
# 只有 active 绑定关系的 drama 计入
```

### 3.5 归一化与软上限

```
if actor_raw_score == 0:
    Heat = 0
else:
    heat_raw = actor_raw_score / normalizer       # normalizer = 全平台活跃演员中位数
    Heat = HEAT_CAP × heat_raw / (heat_raw + HEAT_CAP - 1)
    Heat = round(Heat, 4)
```

| 参数 | V1 值 | 说明 |
|---|---|---|
| `HEAT_CAP` | 8 | 软上限，防止爆款无限放大 |
| `HALF_LIFE_DAYS` | 7 | 衰减半衰期 |
| `LOOKBACK_DAYS` | 30 | 回溯天数 |
| `normalizer` | 动态 | 全平台活跃演员 raw_score 中位数 |

**无数据演员**不进入中位数样本，否则中位数变为 0。

### 3.6 每日批处理流程

```
**# T = 批处理运行的当天日期；处理的是 T-1 日（上一完整自然日）的数据**

1. 锁定上一统计日 (T-1) 的所有播放/完播/互动事件
2. 反作弊过滤 → 生成 drama_daily_metrics (T-1)
3. 计算 drama_day_score
4. 读取 actor_drama_binding 中 active 关系
   → 新绑定关系需回填从剧发布时间到 T-1 的历史分数
5. 对每个 active actor_id，聚合最近 LOOKBACK_DAYS 的带衰减原始分
6. 计算全平台 active actor_set 的中位数 → normalizer
7. 按软上限公式计算 Heat → 写入 actor_heat_daily
8. 挖矿服务读取 actor_heat_daily 最新一条 heat
   （无 Heat 快照的演员按 Heat=0 处理）
```

### 3.7 计算示例

某剧某日：有效播放 10,000、有效完播 3,000、点赞 500、收藏 200、评论 100

```
drama_day_score =
  10,000 × 1.0 + 3,000 × 5.0 + 500 × 2.0 + 200 × 3.0 + 100 × 4.0
= 27,000
```

绑定 A、B 两个演员 → A 和 B 各获得 27,000。假设 A 的衰减原始分为 27,000，normalizer=20,000，HEAT_CAP=8：

```
heat_raw = 27,000 / 20,000 = 1.35
Heat = 8 × 1.35 / (1.35 + 8 - 1) = 1.2934
```

---

## 4. 咖位体系与升级合成

### 4.1 五级咖位参数

| 咖位 | 挖矿系数 | 片酬系数 | 体力上限 | 一键加满价格 |
|---|---|---|---|---|
| Lv1 群演 | 1.0× | 1.0× | 168h | 15 USDC |
| Lv2 配角 | 2.2× | 2.4× | 168h | 36 USDC |
| Lv3 主角 | 5.0× | 5.8× | 168h | 87 USDC |
| Lv4 巨星 | 11× | 13.5× | 168h | 203 USDC |
| Lv5 顶流 | 24× | 31× | 168h | 465 USDC |

**说明：**
- 挖矿系数 → 用于单卡小时权重计算（见第 6 章）
- 片酬系数 → 仅用于计算一键加满体力价格
- 体力上限所有咖位一致，均为 168 小时（10,080 分钟）
- 体力消耗速度所有咖位一致，均为 1 点/小时

### 4.2 升级合成规则

**3 合 1：** 3 张同演员、同咖位 NFT → 销毁后铸造 1 张高一级 NFT。

| 升级路径 | 累计完播门槛 | 合成费 |
|---|---|---|
| Lv1 → Lv2 | ≥ 10,000 | 10 USDC |
| Lv2 → Lv3 | ≥ 50,000 | 25 USDC |
| Lv3 → Lv4 | ≥ 200,000 | 60 USDC |
| Lv4 → Lv5 | ≥ 1,000,000 | 140 USDC |

**合成条件：**
1. 累计完播是演员 ID 维度数据（非单卡维度）
2. 合成前必须解除质押
3. 合成后新卡体力为 0，需休息恢复或一键加满
4. 合成不可逆，Lv5 为 V1 最高等级

**合成链成本：**

| 目标 | 需 Lv1 卡数 | 累计合成费 |
|---|---|---|
| 1 张 Lv2 | 3 | 10 USDC |
| 1 张 Lv3 | 9 | 55 USDC |
| 1 张 Lv4 | 27 | 225 USDC |
| 1 张 Lv5 | 81 | 815 USDC |

### 4.3 权益份额原则

若未来 Vault 开放分配，采用权益份额制（不是按卡张数）：

| 咖位 | 权益份额 |
|---|---|
| Lv1 | 1 份 |
| Lv2 | 3 份 |
| Lv3 | 9 份 |
| Lv4 | 27 份 |
| Lv5 | 81 份 |

升级后卡数减少，但权益份额按 3^level 增长，保持等值。

---

## 5. 体力子系统

### 5.1 基础规则

| 项目 | 规则 |
|---|---|
| 体力上限 | 168 点（= 168 小时 = 10,080 分钟） |
| 消耗速度 | 质押状态：1 点/小时（建议后端按分钟计） |
| 恢复方式 | 解除质押后休息：1 点/小时自动恢复 |
| 一键加满 | 支付 USDC 直接恢复到 168 点，按咖位固定价格 |
| 部分购买 | 不支持，只能一键加满 |

### 5.2 体力状态判定

```
有效挖矿条件（同时满足）：
  1. NFT 处于质押状态
  2. 剩余体力 > 0
  3. Heat > 0
  4. trust > 0

任一条件不满足 → 该时间段产出为 0
```

**体力余量不影响产出倍率：** 168 点、80 点、1 点体力，同条件下每小时产出相同。

### 5.3 体力消耗实现

```
# 按分钟消耗
# **体力精确到分钟；结算时按分钟汇总，不满整小时按比例计算产出**
每分钟遍历所有 active 质押记录：
    if 剩余体力 > 0:
        剩余体力 -= 1
        if 剩余体力 == 0:
            标记该卡为无效挖矿状态
            记录 stamina_log (change_type='CONSUME', change_amount=-1, balance_after=剩余体力)
```

### 5.4 体力恢复

```
# 解除质押后自动恢复（按分钟）
每经过 1 分钟：
    if NFT 未质押 AND 剩余体力 < 10080:
        剩余体力 += 1
        记录 stamina_log (change_type='REST', change_amount=1, balance_after=剩余体力)
```

### 5.5 一键加满

```
function rechargeStamina(actor_id, token_id, level):
    price = LEVEL_RECHARGE_PRICE[level]   -- 固定价格
    用户支付 price USDC
    设置剩余体力 = 10080
    记录 stamina_log (change_type='RECHARGE')
```

| 咖位 | 价格 |
|---|---|
| Lv1 | 15 USDC |
| Lv2 | 36 USDC |
| Lv3 | 87 USDC |
| Lv4 | 203 USDC |
| Lv5 | 465 USDC |

提前加满不会提高产出，体力越接近 0 时加满，性价比越高。

---

## 6. 质押挖矿与周结算

### 6.1 质押规则

| 项目 | 规则 |
|---|---|
| 质押上限 | 每个用户最多 5 张 NFT |
| 同演员多卡 | 允许质押同演员 ID 的多张不同 NFT |
| 限制方式 | 平台账号 + 钱包地址双重限制 |
| 链上转走 | 若质押中的 NFT 被链上转走，平台自动取消质押 |

### 6.2 单卡单位权重（V1）

```
单卡小时权重 = 挖矿系数 × Heat × CP_系数 × trust

V1 固定参数：
  CP_系数 = 1    （CP 字段保留但不启用）
  trust = 1.0    （正常用户；作弊用户 < 1.0）
```

**体力不进入权重公式。** 体力只决定该卡在结算周期内有多少时间处于有效挖矿状态。

### 6.3 名义产出计算

```
R_base = 1  STORY / 单位权重 / 小时

**# 体力按分钟跟踪，产出结算建议按分钟求和后折算为小时**
**# 单卡分钟权重 = 单卡小时权重 / 60**

单卡小时名义产出 = 单卡小时权重 × R_base
**单卡分钟名义产出 = 单卡小时权重 × R_base / 60**

**单卡周期名义产出 = Σ (单卡分钟权重 × R_base × 该分钟是否有效)**
**                 = Σ (单卡小时权重 × R_base / 60 × 该分钟是否有效)**
**                 其中"该分钟是否有效" = 1（满足全部有效挖矿条件）或 0（任一条件不满足）**

**# 等效简化写法：**
**单卡周期名义产出 = 单卡小时权重 × R_base × (有效挖矿分钟数 / 60)**
**                 （有效挖矿分钟：该卡处于质押 + 体力>0 + Heat>0 + trust>0 的累计分钟数）**

个人名义产出 = 该用户所有质押卡在结算周期内的名义产出之和
```

### 6.4 周硬顶缩放

```
每周实际发放受当周硬顶约束。

if 全网名义产出 ≤ 当周硬顶:
    用户实得 = 用户名义产出
    未发出的剩余额度不发放、不回流、不补分
else:
    用户实得 = 用户名义产出 × (当周硬顶 / 全网名义产出)
```

### 6.5 单地址周封顶

```
单地址每周最多领取 = 当周硬顶 × 5%

if 用户缩放后实得 > 单地址封顶:
    用户实得 = 单地址封顶
    超出部分不发放、不回流、不补分
```

### 6.6 结算批次处理流程

```
每周 UTC 周日 00:00 触发结算：

**# 体力按分钟跟踪 → 结算按分钟聚合，确保精度**

1. 确定结算周期：[上周一 00:00 UTC, 上周日 23:59 UTC)
2. 读取该周期内所有 active/unstaked 质押记录
3. 对每张卡，按分钟粒度汇总有效挖矿分钟数：
   a. 查询 actor_heat_daily 取结算周期内每日的 Heat
   **b. 汇总该卡满足全部条件（质押 + 体力>0 + Heat>0 + trust>0）的有效分钟数**
   **c. 单卡周期名义产出 = 单卡小时权重 × R_base × (有效挖矿分钟数 / 60)**
4. 汇总全网名义产出
5. 计算缩放比例 = min(1, 当周硬顶 / 全网名义产出)
6. 缩放个人产出 → 再判断单地址封顶 → 写入 weekly_reward
7. 更新用户可领取余额
```

### 6.7 计算示例

假设：
- 周硬顶 = 100,000 STORY
- 单地址封顶 = 5,000 STORY

| 场景 | 全网名义产出 | 缩放比例 | 用户 A 名义 | 用户 A 实得 |
|---|---|---|---|---|
| 未触顶 | 80,000 | 未缩放 | 134 | 134 |
| 触顶 | 250,000 | 40% | 10,000 | 4,000 |
| 触顶+封顶 | 250,000 | 40% | 20,000 → 8,000 | 5,000 |

---

## 7. 邀请挖矿

### 7.1 子池结构

```
NFT 挖矿池 (550M STORY)
├── 常驻质押池 (75%) → 412.5M STORY
└── 邀请子池 (25%) → 137.5M STORY
```

- 邀请奖励来自独立邀请子池，**不从被邀请人收益中扣除**
- 被邀请人自己挖到的 STORY 不受影响

### 7.2 层级比例

| 层级 | 比例 | 说明 |
|---|---|---|
| 1 级 | 10% | 直接邀请 |
| 2 级 | 5% | 间接邀请 |
| 3 级 | 3% | |
| 4 级 | 2% | |
| 5 级 | 1% | |

**计算基数：** 按被邀请人本期**实得** STORY 计算（不是名义产出）

**计算公式：**

```
某邀请人邀请奖励 = Σ (各层级有效下线的实得 STORY × 对应层级比例)
```

### 7.3 子池硬顶

- 邀请子池有**独立周硬顶**
- 触顶后按份额等比缩减，永不超发
- 邀请子池的周硬顶 = 挖矿池周硬顶 × 25%

### 7.4 有效下线判定

```
满足以下任一条件视为有效下线：
  1. 被邀请人实际挖到过 STORY（有实际领取记录）
  2. 被邀请人发生过 USDC 付费行为

空号注册 → 不产生奖励
自邀请、关联账号 → trust 降低，奖励扣减
```

### 7.5 邀请关系

- 关系一经建立不可更改（最多 5 级）
- 被邀请人作弊扣减收益时，邀请奖励同步扣减

---

## 8. STORY 释放曲线

### 8.1 挖矿池释放

```
M = 550,000,000 STORY   （NFT 挖矿池总量）
a = 20%                  （首年释放比例）
r = 0.8                  （年衰减系数）

第 n 年计划释放 = M × a × r^(n-1)

第 1 年：550M × 20% = 110M STORY
第 2 年：110M × 0.8 = 88M STORY
第 3 年：88M × 0.8 = 70.4M STORY
...
```

### 8.2 周硬顶计算

```
初始周硬顶 = M × a / 52 ≈ 2,115,385 STORY

第 k 周硬顶 = 初始周硬顶 × 0.99572^(k-1)
其中 0.99572 = 0.8^(1/52)    （周衰减系数）
```

**注意：**
- 实际释放 ≤ 计划释放（受周硬顶缩放 + 单地址封顶约束）
- 邀请子池独立周硬顶 = 挖矿池周硬顶 × 25%

### 8.3 全量分配

| 对象 | 比例 | 数量 | 释放机制 |
|---|---|---|---|
| NFT 挖矿 | 55% | 550M | 按周释放，逐年指数递减 |
| 团队 | 15% | 150M | 12 个月 cliff + 36 个月线性 |
| 投资人 | 10% | 100M | 6 个月 cliff + 24 个月线性 |
| Launchpad+流动性 | 10% | 100M | TGE 即可用 |
| 国库 | 5% | 50M | 按季度分批释放 |
| 市场运营 | 5% | 50M | 按运营节奏释放 |

---

## 9. 核心参数速查表

### 9.1 挖矿参数

| 参数 | V1 值 | 说明 |
|---|---|---|
| `R_base` | 1 STORY/单位权重/小时 | 基准产出率 |
| 质押上限 | 5 张/用户 | 账号+钱包双重限制 |
| 周结算 | UTC 周日 00:00 | 每周触发 |
| CP 系数 | 1（V1 不启用） | 保留字段 |
| 权益份额基数 | 3^level | Lv1=1, Lv2=3, Lv3=9, Lv4=27, Lv5=81 |

### 9.2 Heat 参数

| 参数 | V1 值 | 说明 |
|---|---|---|
| `HEAT_CAP` | 8 | 软上限 |
| `HALF_LIFE_DAYS` | 7 | 衰减半衰期（天） |
| `LOOKBACK_DAYS` | 30 | 回溯天数 |
| 单剧最多绑演员 | 5 | — |
| 绑定窗口 | 7×24 小时 | 数据追溯至剧发布时间 |
| 权重版本 | `data_version` | 每次调整权重需记录版本号 |

### 9.3 Heat 数据项权重

| 数据项 | 权重 |
|---|---|
| `valid_play` | × 1.0 |
| `valid_complete` | × 5.0 |
| `like` | × 2.0 |
| `favorite` | × 3.0 |
| `comment` | × 4.0 |

### 9.4 体力参数

| 参数 | 值 |
|---|---|
| 体力上限 | 168 点（10,080 分钟） |
| 消耗速率 | 1 点/小时（质押时） |
| 恢复速率 | 1 点/小时（休息时） |
| 加满方式 | 仅支持一键加满 |

### 9.5 咖位参数

| 咖位 | 挖矿系数 | 片酬系数 | 加满价格 | 合成至该级累计费 |
|---|---|---|---|---|
| Lv1 | 1.0 × | 1.0 × | 15 USDC | — |
| Lv2 | 2.2 × | 2.4 × | 36 USDC | 10 USDC |
| Lv3 | 5.0 × | 5.8 × | 87 USDC | 55 USDC |
| Lv4 | 11 × | 13.5 × | 203 USDC | 225 USDC |
| Lv5 | 24 × | 31 × | 465 USDC | 815 USDC |

### 9.6 升级门槛

| 升级路径 | 累计完播门槛 |
|---|---|
| Lv1 → Lv2 | ≥ 10,000 |
| Lv2 → Lv3 | ≥ 50,000 |
| Lv3 → Lv4 | ≥ 200,000 |
| Lv4 → Lv5 | ≥ 1,000,000 |

### 9.7 分配 & 收入

| 项目 | 比例/值 |
|---|---|
| Mint/版税拆分 | 发行人 40% / 平台 30% / Vault 30% |
| Vault 用途 | V1 沉淀不分配 |
| 二级版税率 | 10% |
| 后续普通发行量 | 10,000 张 |
| 联合曲线售罄倍率 | 20 × P0 |
| 单剧绑演员上限 | 5 |

### 9.8 邀请参数

| 项目 | 值 |
|---|---|
| 邀请子池占比 | 挖矿池的 25% |
| 层级 | 最多 5 级 |
| 比例 | 10%/5%/3%/2%/1% |
| 计算基数 | 被邀请人实得 STORY |

### 9.9 STORY 释放参数

| 项目 | 值 |
|---|---|
| 总量 | 1,000,000,000 |
| 挖矿池 | 550,000,000 (55%) |
| 常驻质押池 | 412,500,000 (挖矿池的 75%) |
| 邀请子池 | 137,500,000 (挖矿池的 25%) |
| 首年释放 | 20% |
| 年衰减 | × 0.8 |
| 初始周硬顶 | ≈ 2,115,385 STORY |
| 周衰减 | × 0.99572 |
| 单地址封顶 | 当周硬顶的 5% |

---

## 10. 风控与边界条件

### 10.1 trust 系数

| 状态 | trust 值 | 影响 |
|---|---|---|
| 正常用户 | 1.0 | 正常挖矿 |
| 轻微异常 | 0.3 ~ 0.9 | 产出按比例缩减 |
| 严重作弊 | 接近 0 | 产出归零，可冻结未领取收益 |

trust 为**中心化**参数，结果可用于收益计算，但具体规则不公开。

### 10.2 边界场景处理

| 场景 | 处理方式 |
|---|---|
| **Heat = 0** | 该卡产出为 0（即使质押+体力充足） |
| **体力 = 0** | 该卡产出为 0（即使质押+Heat>0） |
| **trust = 0** | 该卡产出为 0 |
| **质押中 NFT 被链上转走** | 平台取消质押，原用户停止收益；转移前已确认数据保留；以链上实际转移时间回溯 |
| **Heat 隔日变化** | 已确认的历史挖矿数据不因后续 Heat 变化回滚 |
| **短剧被下架/风控冻结** | 冻结期间 drama_day_score = 0；已有 Heat 贡献可由风控重算剔除 |
| **绑定关系被风控置为 invalid** | 后续批处理不计入该绑定关系；已累计数据可由风控重算 |
| **作弊导致的未领取收益** | 可冻结或扣除 |
| **已领取链上资产** | 不追回，但 trust 降低或清零 |
| **外部市场不支付版税** | 平台未实际收款 → 不产生 40/30/30 分配 |
| **全网名义产出 < 周硬顶** | 未发出的额度不发放、不回流、不补分 |
| **单地址超封顶** | 超出部分不发放、不回流、不补分 |
| **邀请子池触顶** | 按份额等比缩减 |

### 10.3 反作弊重点

- **虚假绑定/恶意蹭数据：** 平台不做前置参演校验，由举报+风控事后处理
- **刷播放/刷完播：** 异常数据不计入 Heat 和累计完播
- **自邀请/女巫攻击：** 通过设备、行为、支付、钱包关联等风控处理
- **数据上链边界：** 播放、Heat、体力、质押、收益计算等**不上链**，中心化处理

### 10.4 经济健康指标 θ（仅供观察）

```
θ = 真实 USDC 现金流 / STORY 增发市值
```

- v2.5 中 θ 仅作为内部观察指标
- **不自动调参**——所有参数调整由团队手动通过版本升级或运营公告执行

---

> **文档版本：** v2.5 · 2026-06-09
>
> **基于《Story.fun 代币经济模型 v2.5》提取编辑**
>
> **适用对象：** 后端开发工程师
>
> **变更记录：** 首次提取编排
