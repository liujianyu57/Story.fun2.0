# 商城与道具 PRD

> 版本：v2.4（草稿，待产品确认）
> 关联：Story.fun 经济模型二期升级方案（第七章～第十一章）
> 本期范围：道具区（4 个道具，全部作为经纪人页功能落地）、账户道具库存
> 不在本期：道具细分类、聚光灯（推广）、收藏展示道具（皮肤/头像框/称号）、限时礼包

---

## 1. 背景与目标

### 1.1 背景

当前 STORY 使用路径过于单向（获得→出售），NFT 角色运营行为偏机械（演出→等待→补体力→重复）。缺少 STORY 内部消费场景与持续性收入。

### 1.2 目标

1. 建立 STORY 的持续消费场景：把角色运营、成长道具化。
2. 把重复操作包装为可管理、可储备、可消费的角色运营资源，从机械操作转向养成/游戏化。
3. 形成平台持续性收入：USDC 购买（体力补给包、训练手册）+ STORY 消耗（Story Claw）。
4. 道具只做账户内持有与消耗，不开放道具二级交易。

### 1.3 落地形态（重要）

本期**不建设独立商城页面**。4 个道具全部作为 **经纪人页** 的功能落地，采用**混合方案（方案 C）**：

| 道具 | 落点与交互 |
|---|---|
| 体力补给包 | 「补充体力」弹窗内改为消耗补给包，并**内嵌购买**（弹窗内显示库存 + 购买，不跳走）|
| 训练手册 | 「升级角色」弹窗内改为消耗训练手册，并**内嵌购买**（弹窗内显示库存 + 购买）|
| Story Claw（周/月卡）| **独立底部 sheet**：激活（周/月卡选择）+ 三自动化开关 + 触发阈值 + 剩余天数 + +5% 状态 |
| 道具总览/库存 | **桌面端**：通用标题栏（desktop-header）🎒 图标 → 道具 sheet；**移动端**：页面顶部常驻区 🎒（与 Claw 🐾 并列）。「更多」菜单保留入口作辅助 |

道具 sheet 只平铺展示 4 个道具，**不做分类**。

### 1.4 交互设计原则（参考 StepN / Axie，移动端优先）

1. **上下文内嵌购买**：高频消耗品（补给包、手册）的购买按钮长在"使用场景"（补充体力/升级弹窗）里，不打断流程、不跳页面。
2. **入口显眼**：**桌面端**道具/Claw 入口放**通用标题栏**（desktop-header，全站页面可见，Claw 到期提醒全局生效）；**移动端**放页面顶部常驻区（🎒 图标与 Claw 🐾 并列）。「更多」菜单入口保留作辅助。
3. **卡片即入口**：角色状态（体力低/可升级）直接引出操作，不靠导航。
4. **全局状态常驻**：Claw 生效状态常驻（桌面端通用标题栏 / 移动端页面顶部），如 `🐾 Claw · 剩 23 天 · +5%`，点击可打开 Claw sheet（类似 StepN 能量条）。
5. **一键联动**：现有「一键补充」（批量）改为消耗补给包补满；库存不足时弹窗内嵌购买补齐，或选择跳过。
6. **移动端精简**：移动端道具 sheet 与所有弹窗**设计精简、文字精简**（少字段、短文案、单屏可读）。
7. **道具图标化**：库存行与道具卡片均以**图标 + 名称**展示（桌面端/移动端一致）；库存数量一行图标化。

### 1.5 交互示意图

**图 1A：经纪人页入口地图 · 桌面端**

```
┌─────────────────────────── 经纪人页（桌面端）─────────────────────────────┐
│  ┌─ 通用标题栏（全站 desktop-header）────────────────────────────────┐ │
│  │  [Logo]  搜索…            [🎒道具] [🐾Claw·剩23天·+5%]  [通知][头像] │ │
│  └──── 🎒道具 / 🐾Claw 为账号级入口，全站页面可见（Claw 到期提醒全局生效）─┘ │
│                                                                            │
│  演出中（5 槽）              待办面板            升级面板                  │
│  [卡][卡][卡][卡][卡]      [待办 N]           [✦ 升级 N]                 │
│      │                        │                   │                        │
│      ▼                        ▼                   ▼                        │
│  卡上操作：               待办弹窗            升级角色弹窗                 │
│  体力低→补满              (签到/任务)         (消耗训练手册×N)           │
│  (消耗补给包)                                  │ 手册不足→弹窗内嵌购买      │
│                                                                            │
│  ┌─ 批量操作栏 ────────────────────────────────────────────────────────┐  │
│  │  [一键演出]  [一键补充]  [一键休息]                                 │  │
│  └── 一键补充 → 自动消耗补给包（库存不足 → 弹窗内嵌购买补齐）─────────┘  │
└────────────────────────────────────────────────────────────────────────────┘
```

**图 1B：经纪人页入口地图 · 移动端**

```
┌─────────────────────────── 经纪人页（移动端）─────────────────────────────┐
│  ┌─ 顶部 ─────────────────────────────────────────────────────────────┐ │
│  │  [🎒道具] [🐾Claw·+5%]          [待办] [✦升级] [≡]                │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│  演出中（焦点卡）                                                          │
│  [ 卡 ]                                                                   │
│      │                                                                    │
│      ▼                                                                    │
│  卡上操作：体力低→补满（消耗补给包）                                      │
│  库存不足 → 弹窗内嵌购买补齐                                              │
│                                                                            │
│  ┌─ 底部候场横栏（可展开）──────────────────────────────────────────────┐ │
│  │  [候场卡][候场卡][候场卡]…   [∧全部]                                │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│  ≡ 更多菜单：规则 / 奖池 / 道具（辅助入口）                                │
│  🎒 顶部道具图标 → 道具 sheet（主入口，移动端+桌面端一致）                 │
└────────────────────────────────────────────────────────────────────────────┘
```

**图 2：道具 sheet（背包 = 商城，仅查看库存 / 购买 / Claw 激活，不提供"使用"跳转）**

```
┌─────────────── 道具 sheet（底部抽屉，≡更多 → 道具）───────────────┐
│                                                                     │
│  我的库存：[🧃]×12  [📘]×3  [🐾周]×2  [🐾月]×0                    │
│            ↑ 库存图标化一行展示（桌面/移动端一致）                  │
│                                                                     │
│  ┌─ 🧃 体力补给包 ─────────────────────────┐                       │
│  │ 单价 10 USDC   │  [- 数量 +]  │ [购买]   │                       │
│  └──────────────────────────────────────────┘                       │
│  ┌─ 📘 训练手册 ───────────────────────────┐                       │
│  │ 单价 0.1 USDC  │  [- 数量 +]  │ [购买]   │                       │
│  └──────────────────────────────────────────┘                       │
│  ┌─ 🐾 Story Claw 周卡 ───────────────────┐                       │
│  │ 800 STORY      │  [- 数量 +]  │ [购买]   │  [激活]              │
│  └──────────────────────────────────────────┘                       │
│  ┌─ 🐾 Story Claw 月卡 ───────────────────┐                       │
│  │ 3000 STORY     │  [- 数量 +]  │ [购买]   │  [激活]              │
│  └──────────────────────────────────────────┘                       │
│  ※ 道具卡片名称前均带图标；此 sheet 只负责                             │
│    查看库存 / 购买 / Claw 激活，使用从对应功能弹窗进入               │
└──────────────────────────────────────────────────────────────────────┘
```

**图 3：Claw sheet（激活 + 自动化设置）**

```
┌─────────────── Claw sheet（顶部🐾标识 或 道具 sheet 点激活）────────┐
│                                                                     │
│  Story Claw · 周卡 800 STORY / 月卡 3000 STORY                      │
│                                                                     │
│  当前状态：🟢 激活中 · 剩 23 天 · 所有角色的每小时产出 +5%         │
│  [激活周卡 +7天]  [激活月卡 +30天]   ← 库存不足时置灰+提示            │
│                                                                     │
│  ── 自动化开关（☑=开 ☐=关）──────────────────────────────────   │
│  ☑ 自动补体力（默认开）  阈值: [0~168h]（默认 0）                  │
│  ☑ 0体力自动休息（默认开 · 结束演出召回）                          │
│  ☐ 自动最优演出（默认关 · 用户手动开启）                           │
│  [保存设置]                                                          │
└──────────────────────────────────────────────────────────────────────┘
```

**图 4：内嵌购买（高频消耗品，弹窗内完成"看缺口→补货→使用"，不跳走）**

```
┌─ 补充体力弹窗 ─────────────────────────────┐
│  状态 A：库存充足                          │
│  Lv3 主角 · 体力 100/168                   │
│  补满需：补给包 ×5  ｜ 库存 ×12 ✓          │
│  [消耗 5 个 · 补满]                        │
├────────────────────────────────────────────┤
│  状态 B：库存不足 → 出现购买入口            │
│  Lv3 主角 · 体力 100/168                   │
│  补满需：补给包 ×5  ｜ 库存 ×2             │
│  ⚠ 还差 3 个                              │
│  [购买补给包] ← 点击后在弹窗内展开 ↓        │
├────────────────────────────────────────────┤
│  状态 C：弹窗内展开购买面板（不跳走）        │
│  ┌─ 购买补给包 ──────────────────────┐    │
│  │  单价 10 USDC ｜ 数量 [- 3 +]     │    │
│  │  合计 30 USDC                     │    │
│  │  [确认支付（模拟）]  [取消]       │    │
│  └───────────────────────────────────┘    │
│  支付成功 → 库存 ×2→×5 → 面板收起          │
├────────────────────────────────────────────┤
│  状态 A（回到）：库存 ×5 ✓                 │
│  [消耗 5 个 · 补满] → 168/168              │
└────────────────────────────────────────────┘
```

升级角色弹窗同样适用（手册缺口 → 内嵌购买手册 → 确认升级）。

**图 5：升级列表弹窗卡片（4.3）**

```
┌─ 升级列表弹窗 ─────────────────────────────────────────────┐
│  角色名 #12           Lv2 ──→ Lv3                          │
│  片酬：9.0 → 27.0 STORY/h                                  │
│  ┌─ 升级条件 ───────────────────────────────────────────┐  │
│  │  [📘 训练手册 ×200]  [库存 150 ✗]   [购买]           │  │
│  │  [👥 同IP同等级角色]  [1/2 ✗]       [去市场]         │  │
│  └───────────────────────────────────────────────────────┘ │
│  [✦ 立即升级]（条件不足时置灰，点击提示缺口）              │
└────────────────────────────────────────────────────────────┘
```

**图 6：刷剧宝箱（4.4）**

```
┌─ 短剧播放页 ──────────────────────────────────────────────┐
│  🎁 ← 播放器左上角悬浮宝箱（点击展开面板）                │
│                                                           │
│  ┌─ 宝箱面板 ────────────────────────────────────────┐   │
│  │  🎁 刷剧宝箱 · 生命周期已领 12/60                  │   │
│  │  ─────────────────────────────────────────         │   │
│  │  ☑ 看满 1 分钟 → +1 本    [已领取]                 │   │
│  │  ☐ 看满 20 分钟 → +1 本   [进度 12'30"]            │   │
│  │  今日已领 1 本 · 每日 00:00 (UTC) 重置              │   │
│  └────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────┘
```

**图 7：签约激励进度（4.5，actors 市场页）**

```
┌─ 角色 IP 市场（actors.html）──────────────────────────────┐
│  签约送 Claw 周卡：已签 3/5                                │
│  ▓▓▓▓▓▓▓░░░░░   再签 2 张解锁 7 天自动运营 + 5% 产出      │
│  [去签约]                                                 │
└────────────────────────────────────────────────────────────┘
```

---

## 2. 道具区全局规则

| # | 规则 | 说明 |
|---|---|---|
| 1 | 标准化道具 + 固定单价 | 每个道具固定单价，无溢价、无竞拍 |
| 2 | 批量购买 | 可一次购买多个同种道具，存入账户库存（无单次/单日数量上限）|
| 3 | 消耗按场景计 | 实际使用时按 NFT 等级 / 服务周期消耗不同数量 |
| 4 | 道具不可二级交易 | 道具仅在账户内持有与消耗，不可转让、不可交易 |
| 5 | 价格不追溯 | 价格以购买时为准，不追溯差价；已购库存不受未来调价影响 |
| 6 | 库存持久化 | 未使用道具持续保存在账户库存，不清零、不过期（Claw 激活后有效期除外）|
| 7 | 背包即商城 | 道具 sheet 内可直接购买并囤货（仅自用；购买规则见 #2、#4、#6）|

---

## 3. 道具总览（本期 4 个）

| 道具 | 支付资产 | 单价 | 核心效果 | 消耗方式 |
|---|---|---|---|---|
| 体力补给包 | USDC | 10 USDC/个 | 补满 NFT 工作时长至 168h | 按 NFT 等级固定消耗数量 |
| Story Claw 周卡 | STORY | 800 STORY | 7 天自动运营 + 账号名义产出 +5% | 激活扣 1 张，有效期 +7 天 |
| Story Claw 月卡 | STORY | 3000 STORY | 30 天自动运营 + 账号名义产出 +5% | 激活扣 1 张，有效期 +30 天 |
| 训练手册 | USDC | 0.1 USDC/本 | NFT 升级的标准成长材料（小额高频）| 升级时按路径固定消耗数量（100/200/400/800）|

---

## 4. 道具详细规格

### 4.1 体力补给包

**定位**：恢复角色工作时长的标准补给材料，替代现有"付费直接补体力"。

- 只出售一种补给包，无等级/款式区分。
- 单价：固定参考价 **10 USDC/个**。
- 使用：对 NFT 执行一次"补满至 168 小时"操作，**按 NFT 等级固定消耗**（不按差额计费，已确认）：

| NFT 等级 | 补满至 168h 所需数量 | 等效原成本 |
|---|---|---|
| Lv1 | 1 个 | 10 USDC |
| Lv2 | 2 个 | 20 USDC |
| Lv3 | 5 个 | 50 USDC |
| Lv4 | 13 个 | 130 USDC |
| Lv5 | 32 个 | 320 USDC |

**规则与边界**
- 库存不足时禁止补满操作，提示缺少数量（如"还差 3 个体力补给包"）。
- 补满目标固定 168h；即使当前工作时长接近 168h，仍按等级固定消耗（已确认）。
- **任意状态角色均可补满**（演出中/候场/休息），补满后进入演出则按 168h 计。
- 使用后从库存扣减。

**经纪人页落点**：`补充体力`弹窗——"付费补满"改为"消耗补给包补满"，弹窗内展示当前库存 + 所需数量 + 「购买补给包」内嵌购买入口。**购买面板为弹窗内嵌二级视图（展开/收起，不关闭当前弹窗），数量默认=缺口数，支付成功后库存原地刷新并收起面板**。

**一键补充联动**：现有「一键补充」（批量操作）改为按 NFT 等级消耗补给包补满；确认时显示**预计总消耗**（如"将消耗 12 个补给包"）；库存不足时**弹窗内嵌购买**（默认补齐缺口数），用户也可选择跳过部分角色。

### 4.2 Story Claw（周卡 / 月卡）

**定位**：账户级自动角色运营助手 + 账号级产出增益（承接原能量饮料效果）。

#### 4.2.1 规格与价格

| 规格 | 有效期 | 价格（STORY）|
|---|---|---|
| 周卡 | 连续 7 天 | 800 |
| 月卡 | 连续 30 天 | 3000 |

- 周卡与月卡**效果完全一致**，仅有效期与价格不同。
- 购买后进入账户库存（未激活状态）。

#### 4.2.2 激活与延长

- 激活：消耗库存中 1 张卡，生效期按规格增加（周卡 +7 天、月卡 +30 天）。
- 首次激活从激活时刻起算连续天数。
- 重复激活（同规格或混用）只延长有效期，**不叠加功能倍率、不叠加产出增益**。
- 持有数量不限，库存可同时存在周卡与月卡。
- 激活入口：道具 sheet 内激活；激活期间也可点击通用标题栏/页面顶部的常驻标识直接进入 Claw sheet。
- **未激活 / 到期后**：顶部标识不显示，统一从**道具 sheet** 进入 Claw sheet（激活 / 续费）。
- **购买后引导**：购买成功弹「立即激活？」（周卡/月卡二选一）；库存中未激活卡带「未激活」角标。

#### 4.2.3 产出增益（替代原能量饮料）

Claw 生效期间，**账号名义产出 +5%（账号级）**。

- 作用于账号名下所有正在产出的 NFT 的名义产出。
- 只作用于名义产出，**不修改** 热度（Heat）、片酬系数、P0 价格系数、信誉系数。
- **受全网周硬顶和单地址周硬顶约束**，不绕过硬顶。
- 多张卡（含周/月卡混用）不叠加，始终 +5%。
- 与自动运营无叠加变化：+5% 为固定增益。
- 名义产出展示处**不额外标注** +5%（避免 Claw 中途到期造成的口径切换）；+5% 仅在 Claw sheet 与顶部标识中体现。
- **用户可见文案统一为「所有角色的每小时产出 +5%」**（PRD 内部保留"名义产出"表述）。

#### 4.2.4 自动补体力（默认开）

- 用户设置：自动补给开关、触发阈值（剩余工作时长**降至该值时**触发自动补体力；可调范围 0~168h，默认 0）。
- 阈值语义：默认 0 = **体力耗尽（=0）时才补**（最省道具）；调高阈值 = 提前补（更稳、更耗道具）。
- 触发后消耗账户中的体力补给包，对目标 NFT 执行补满。
- 补给包不足则停止工作（不影响已激活的 Claw 有效期），由「0 体力自动休息」接管。

#### 4.2.5 0 体力自动休息（默认开）

- 某 NFT 工作时长降至 0 时：若自动补体力已开启且库存充足 → **先补满**；若未开启或补给包不足 → **自动结束该 NFT 的演出（召回下台）**。
- 结束演出后进入自然恢复状态（按现有休息恢复规则）。

#### 4.2.6 自动最优演出（默认关，用户手动开启）

- 名义小时权重 = 片酬系数 × 热度（Heat）× P0 价格系数 × 信誉系数（各系数沿用项目现有定义）
- 在账户持有且工作时长 > 0 的 NFT 中排序，自动安排权重最高的**最多 5 张**演出；出现更高权重 NFT 时自动替换组合中权重最低者。

#### 4.2.7 边界与约束

- 不增加 热度（Heat）、不增加 P0 系数、不增加基础奖励率、不增加额外挖矿倍率——仅提升运营自动化与时间效率。
- 用户可手动停用某个自动化能力（开关），不影响有效期。
- 到期后：自动化与产出增益同时失效；未消耗库存卡不受影响，可随时再次激活。
- **到期后角色行为**：Claw 已安排的演出组合**保留**（继续跑完体力）；体力=0 时回到无 Claw 的默认行为（停止产出，**不自动召回**）；顶部标识随之消失。
- **到期提醒**：剩余 ≤3 天时顶部标识变色/加红点；到期当天提示「Claw 已到期，续费激活？」（**固定推荐续月卡**，用户可切换为周卡）。

#### 4.2.8 站内通知（轻量，仅关键事件）

接入现有站内通知体系，只通知 3 类关键事件：

| 事件 | 通知文案示例 |
|---|---|
| 自动补体力触发但补给包不足，自动停止 | 「补给包不足，自动补体力已停止」|
| 0 体力自动休息（召回下台）| 「角色 {X} 体力耗尽，已自动结束演出」|
| Claw 到期前 1 天 / 当天 | 「Claw 将于明天到期，续费继续享 +5%」|

- 自动最优演出替换组合**不通知**（避免噪音）。
- 通知归入现有通知体系（系统 Tab，新增 `claw` 类目或并入角色管理类）。

**与现有通知的冲突处理（重要）**：
- 项目现有「角色管理」类已含"体力不足，尽快补充"通知（体力=0 时触发）。
- **Claw 生效期间**：体力=0 且无补给包时 → **抑制现有"体力不足"通知**，只发 Claw 召回通知（避免"快去补充"与"已自动结束演出"两条矛盾通知）。
- **Claw 到期后**：恢复现有"体力不足"通知逻辑。

**经纪人页落点**：独立**底部 sheet**（移动端抽屉式）：Claw 激活（周卡/月卡切换 + 购买 + 库存）、三自动化开关、自动补体力触发阈值设置、剩余天数、+5% 产出增益标识；常驻小标识（桌面端**通用标题栏** / 移动端**页面顶部**，`🐾 Claw · 剩 N 天 · +5%`），点击打开 sheet。**常驻标识仅在激活期间显示**（未激活不显示，避免占位噪音）。

### 4.3 训练手册

**定位**：NFT 升级的标准成长材料。升级不再按等级直接付钱，改为消耗固定单价的训练手册，以数量体现升级成本差异。手册为小额高频材料（0.1 USDC/本），主要靠购买，刷剧宝箱提供少量免费来源。

- 单价：固定参考价 **0.1 USDC/本**。
- 升级条件：**消耗 2 张同 IP 同等级角色 + 训练手册**（无累计完播门槛）。**消耗的 2 张角色由用户选择**（沿用现有升级选择交互，展示每张等级/热度（Heat）/已装备项，并提示"该卡当前体力 Xh，消耗后不返还"）。
- 升级消耗：

| 升级路径 | 训练手册消耗 | 对应原升级成本 |
|---|---|---|
| Lv1 → Lv2 | 100 本 | 10 USDC |
| Lv2 → Lv3 | 200 本 | 20 USDC |
| Lv3 → Lv4 | 400 本 | 40 USDC |
| Lv4 → Lv5 | 800 本 | 80 USDC |

- 库存不足时禁止升级操作，提示缺少数量。
- 获取途径：**道具区购买 + 刷剧宝箱**（见 4.4；宝箱仅提供小额补充，升级主要靠购买）。

**经纪人页落点**：`升级角色`弹窗（现有升级条件/升级费用区）——升级费用由"按等级付 USDC"改为"消耗训练手册"，弹窗内展示库存 + 所需数量 + 「购买训练手册」内嵌购买入口。

**升级列表弹窗卡片**（每个角色卡片）：
- **去掉「参演短剧完播」条件**（标签 + 状态 + 进度条一并移除）。
- 新增**「训练手册 ×N」条件**：标签 + 状态（库存足够 ✓ / 不足 ✗）+ 进度（手册库存/所需数量）。
- 保留**「同 IP 同等级角色」条件**（have/2）。
- 两个条件均满足 → 「✦ 立即升级」可点；任一不足 → 点击提示缺口（手册不足 → 弹窗内嵌购买；**角色不足 → 跳转 actors 市场页获取角色**）。

### 4.4 刷剧宝箱

**定位**：看剧领手册的日活钩子。低门槛、低发放量，严格控制免费发放（不冲击手册 USDC 收入）。

**规则**：
- 入口：短剧播放页 → **播放器左上角悬浮宝箱图标**（不遮挡主要控件，移动端适配小尺寸），点击打开宝箱面板。
- 宝箱面板内容：两个里程碑档位及进度（「看满 1 分钟 → 1 本」「看满 20 分钟 → 再 1 本」）、当日领取状态、生命周期剩余额度（如"已领 12/60"）。
- 单日看满 **1 分钟 → 奖励 1 本训练手册**；单日看满 **20 分钟 → 再奖励 1 本**（两个里程碑，各 1 次/日）。
- **每天 00:00 (UTC) 重置**，重置后重新累计观看时长。
- **用户生命周期累计上限 60 本**（仅计宝箱发放），达到后**宝箱不再显示**。
- 奖励**达标后自动入库存**，宝箱面板同步显示"已领取"；入库存来源=刷剧宝箱。
- 防刷：需真实播放校验（前台播放、进度校验；具体策略实现阶段定）。

**说明**：宝箱单次 1 本（0.1 USDC 价值）为刻意控制——发放过多会稀释训练手册购买需求、影响平台 USDC 收入；宝箱仅作日活激励，升级所需手册（100~800 本）以购买为主。

### 4.5 签约激励：送 Claw 周卡

**定位**：Claw 免费试用转化入口——用签约核心行为换取 7 天 Claw 体验，到期引导续费月卡。

**规则**：
- 用户**累计签约 5 张角色卡**（同 IP 的多张卡均计入；签约动作完成即计数）后，直接赠送 **1 张 Claw 周卡**。
- 发放方式：达成时**自动入库存（未激活）**，并提示「解锁 Claw 周卡，激活体验 7 天自动运营」。
- **用户生命周期最多通过该途径获得 1 张**；达到后不再发放、不再提示。
- 与购买不冲突：仍可正常购买周卡/月卡（购买不受该激励影响）。
- **进度显示位置**：主显示在 **actors.html（角色 IP 市场）**——市场页常驻签约进度条（"签约送 Claw 周卡：已签 N/5"），签约成功时提示解锁；studio 页不单独做进度条（赠送卡入库存后由"未激活角标 + 激活引导"承接）。
- 目的：7 天到期后由到期提醒引导续费（月卡 3000 STORY），完成试用→付费转化。

---

## 5. 库存与账户模型

- 道具库存为**账户级**（非 NFT 级）：购买入库、使用出库。
- 库存字段（建议）：道具 ID、数量、来源（**购买 / 刷剧宝箱 / 签约激励 / 后台发放**）、入库时间。
- Story Claw 区分规格（周卡/月卡）与状态：`未激活` / `激活中（有效期至 xx）`。
- 道具不支持转让、不支持出售、不支持提现。

### 5.1 后台道具发放

- 后台（admin）提供「道具发放」功能：管理员按**用户（昵称/地址）** 选择对象 → 选择**道具**（体力补给包/训练手册/Claw 周卡/Claw 月卡）→ 输入**数量** → 填写**发放理由**（必填）→ 确认发放。
- 发放后**直接入目标用户账户库存**（来源=后台发放）。
- **发放日志**：操作人、时间、对象、道具、数量、理由——可审计、可追溯。
- 用户侧：库存即时 +N；**不发送站内通知**（后台发放为静默操作，用户在道具 sheet 查看库存时可见）。

---

## 6. 支付与定价

| 道具 | 支付资产 | 单价 |
|---|---|---|
| 体力补给包 | USDC | 10 USDC/个 |
| 训练手册 | USDC | 0.1 USDC/本 |
| Story Claw 周卡 | STORY | 800 STORY |
| Story Claw 月卡 | STORY | 3000 STORY |

- 定价规则：固定单价；调价只影响新购买，不影响已购库存。
- 支付失败/取消：不产生库存变动；支付成功回调后入库（防重复回调）。

---

## 7. 核心流程

### 7.1 购买流程（两种入口）

**入口一：上下文内嵌购买（补给包 / 训练手册）**

```
补充体力弹窗 → 「购买补给包」→ 选数量 → 确认金额（USDC）
升级角色弹窗 → 「购买训练手册」→ 选数量 → 确认金额（USDC）
→ 支付成功 → 库存 +N → 弹窗内库存数字即时更新 → 继续原操作
```

**入口二：道具 sheet（全部 4 道具 / Claw）**

```
桌面端通用标题栏 / 移动端页面顶部「🎒 道具」→ 道具 sheet：4 道具平铺（库存 + 单价 + 购买；「更多」菜单入口作辅助）
  ├─ 补给包/手册：购买（USDC）
  └─ Claw 周卡/月卡：购买（STORY）→ 可在 sheet 内直接激活
→ 支付成功 → 库存 +N → sheet 库存数字即时更新
※ 道具 sheet 不提供"使用"跳转（使用从各功能弹窗进入）
```

支付说明：USDC 道具走稳定币支付；STORY 道具走 STORY 支付（余额校验 → 扣款）。**原型阶段（demo）支付流程模拟**（点击确认即完成，不做真实链上支付）。支付失败/取消不产生库存变动，支付成功后入库（防重复提交）。

### 7.2 使用/消耗流程

| 道具 | 入口（经纪人页）| 动作 |
|---|---|---|
| 体力补给包 | 补充体力弹窗 | 点"补满 168h" → 校验库存 → 扣减 → 工作时长=168h；库存不足时弹窗内嵌购买（数量默认缺口数）|
| Story Claw | Claw sheet（底部）| 点"激活" → 扣 1 张（周/月）→ 有效期 +7/+30 天 → 增益与自动化生效 |
| 训练手册 | 升级角色弹窗 | 升级 → 校验 2 张同 IP 同等级角色 + 扣手册 → 升级成功；手册不足时弹窗内嵌购买 |

---

## 8. 与其他系统联动

| 系统 | 联动点 |
|---|---|
| 角色升级系统 | 升级消耗 2 张同 IP 同等级角色 + 训练手册（替代原按等级付 USDC）|
| 体力/工作时长 | 补给包补满 168h；Claw 自动补体力、0 体力自动结束演出（召回）|
| 演出与硬顶 | Claw +5% 只作用于名义产出且受周硬顶约束；自动最优演出按名义小时权重 |
| 后台管理 | 后台道具发放：按用户发放指定道具数量，直接入库存（见 5.1）|
| 站内通知 | Claw 关键事件通知（补给不足/召回/到期，见 4.2.8）|
| 刷剧宝箱 | 短剧播放页看剧领手册：1 分钟 1 本、20 分钟再 1 本，每日重置，生命周期上限 60 本（见 4.4）|
| 签约激励 | 累计签约 5 个不同角色送 Claw 周卡 1 张（未激活入库存），生命周期 1 次（见 4.5）|

---

## 9. 决策记录与剩余待定

### 9.1 已确认决策（本轮）

| 项 | 决策 |
|---|---|
| 支付资产 | **USDC**（与项目现有账本一致；原型阶段为**模拟支付**，不做真实链上支付）|
| 升级消耗 | **2 张同 IP 同等级角色**（名词统一为"角色"，数量沿用现有规则）|
| Claw 补体力阈值 | 可调 **0~168h，默认 0**（默认 0 = 体力耗尽时才补，最省道具）|
| 内嵌购买 | 1 个起步；支付前二次确认 |
| 补给包补满对象 | **任意状态角色均可补**（演出中/候场/休息）|
| +5% 展示 | 名义产出处**不标注**（避免 Claw 中途到期口径切换）；仅在 Claw sheet 与顶部标识体现 |
| Claw 激活入口 | 道具 sheet + 通用标题栏/页面顶部常驻标识均可（未激活/到期后仅道具 sheet）|
| 背包即商城 | 道具 sheet 内可直接购买囤货（无上限、仅自用）|
| 价格 | 以购买时为准，不追溯差价 |
| 训练手册 | 单价 0.1 USDC/本；升级消耗 100/200/400/800 本（总成本 10/20/40/80 USDC 不变）|
| 刷剧宝箱 | 1 分钟 1 本、20 分钟再 1 本，每日重置；生命周期上限 60 本，达到后不显示；单次 1 本为刻意控制（不冲击收入）|
| 签约送周卡 | 累计签约 5 张角色卡（同 IP 多张卡计入）送 Claw 周卡 1 张（未激活入库存 + 激活引导）；生命周期 1 次；进度显示在 actors 市场页；免费试用→续费转化 |

### 9.2 剩余待定（非本期阻塞）

1. 支付二次确认文案的具体措辞——文案/UI 阶段确定。

---

## 10. 分期建议

**P0（先行落地）**
- 道具区框架（经纪人页入口 + 账户库存模型）
- 体力补给包（购买 + 补充体力弹窗改为消耗补给包，按等级固定消耗）
- 训练手册（购买 + 升级弹窗改为消耗手册，0.1 USDC/本、100/200/400/800 本）
- **刷剧宝箱**（看剧领手册，日活钩子）

**P1**
- Story Claw（周卡 800 / 月卡 3000 STORY；激活/延长/三自动化 + 账号 +5% 产出增益，含硬顶约束）

**后续版本（本期不做）**
- 聚光灯（推广道具）
- 收藏展示道具（限定皮肤 / 头像框 / 特殊称号）
- 限时礼包（补给包+手册组合）
- 道具细分类

---

## 附：变更记录

- v2.3 → v2.4（一致性复查）：
  - 修复 6 处引用未同步：1.4 #4、4.2.2、4.2.7（孤立 Heat→热度（Heat））、4.2 落点、7.1 入口二、9.1——全部统一为"桌面端通用标题栏 / 移动端页面顶部"表述。
- v2.2 → v2.3（三检查修订）：
  - **术语对齐项目用词**：等级系数 → **片酬系数**；Heat → **热度（Heat）**；标注"各系数沿用项目现有定义"。
  - **库存来源枚举统一**：购买 / 刷剧宝箱 / 签约激励 / 后台发放。
  - **Claw 未激活/到期后入口明确**：顶部标识仅激活期间显示，未激活/到期统一从道具 sheet 进入（激活/续费）。
  - **后台发放不发送站内通知**（静默操作，道具 sheet 可见）。
  - 升级卡片"角色不足"→ 跳转 actors 市场页。
  - **新增图 5（升级列表卡片）、图 6（刷剧宝箱）、图 7（签约激励进度）**。
- v2.1 → v2.2：
  - **道具/Claw 入口位置明确**：桌面端放**通用标题栏**（desktop-header，账号级入口全站可见，Claw 到期提醒全局生效）；移动端保持页面顶部常驻区（desktop-header 在 ≤768px 隐藏）。更新图 1A、1.3、1.4 #2。
- v2.0 → v2.1：
  - **图 1 拆分**为图 1A（桌面端：顶部导航栏 + 5 槽 + 待办/升级面板 + 批量操作栏）与图 1B（移动端：顶部常驻 + 右上角按钮 + 焦点卡 + 底部候场横栏）。
- v1.9 → v2.0：
  - 签约激励门槛修正：**累计签约 5 张角色卡（同 IP 多张卡均计入）**；进度主显示在 **actors.html 市场页**（常驻进度条 + 签约成功提示），studio 不单独做进度条（由未激活角标 + 激活引导承接）。
- v1.8 → v1.9：
  - **新增 4.5 签约激励**：累计签约 5 个不同角色 → 赠送 Claw 周卡 1 张（未激活入库存 + 激活引导提示）；用户生命周期最多 1 次；定位为 Claw 免费试用→续费转化入口。8 章联动、9.1 决策同步。
- v1.6 → v1.7：
  - **训练手册改价**：10 USDC/本 → **0.1 USDC/本**；升级消耗 1/2/4/8 → **100/200/400/800 本**（单次升级总成本 10/20/40/80 USDC 不变）。更新 3 章、4.3、6 章、图 2、9.1。
  - **新增 4.4 刷剧宝箱**：看 1 分钟 1 本、看满 20 分钟再 1 本，每日 00:00 (UTC) 重置；生命周期上限 60 本，达到后不显示；单次 1 本为刻意控制（不冲击 USDC 收入）；手册获取途径改为"购买 + 刷剧宝箱"。
  - 宝箱入口定位为**播放器左上角悬浮图标**，达标自动入库存；面板显示档位进度/领取状态/生命周期剩余额度。
  - 8 章联动、10 分期（宝箱归入 P0）同步更新。
- v1.5 → v1.6：
  - **Claw 通知与现有通知的冲突处理**：Claw 生效期间体力=0 时**抑制现有「体力不足」通知**（避免与召回通知语义矛盾）；到期后恢复。通知归入系统 Tab 新增 `claw` 类目。
- v1.4 → v1.5：
  - **后台道具发放**（5.1 + 第 8 章联动）：按用户发放指定道具数量，直接入库存；发放理由必填、日志可审计；库存来源新增"后台发放"。
  - **移动端道具入口改到顶部常驻区**（🎒 图标与 Claw 🐾 并列，移动端+桌面端均显示；「更多」菜单保留作辅助）——替代"收进更多菜单"的隐晦设计；更新 1.3、1.4 #2、图 1。
  - **Claw 站内通知**（4.2.8）：仅通知补给包不足停止、0 体力召回、到期前/当天 3 类关键事件；自动最优演出替换不通知。
- v1.3 → v1.4（文档质量优化）：
  - 图 3 自动化开关与正文对齐：自动最优演出标 **☐（默认关）**；开关行标注默认值（补体力/0 体力休息默认开）。
  - 全局规则 #7 精简去重（移除与 #4/#6 重复的"不可交易/库存持久化"，改为引用）。
- v1.2 → v1.3：
  - **升级列表弹窗卡片 UI 明确**：去掉「参演短剧完播」条件，新增「训练手册 ×N」条件（标签 + 状态 + 库存/所需进度）；保留「同 IP 同等级角色」（have/2）；条件不足时点击分别引导（手册不足→内嵌购买；角色不足→获取角色）。
- v1.1 → v1.2（三轮体验评审修订）：
  - 7.1 道具 sheet 流程移除残留的"使用"跳转（与 v1.1 一致，sheet 仅查看/购买/激活）。
  - **Claw 到期后行为明确**：已安排演出组合保留、体力 0 回默认（停产出不自动召回）、顶部标识消失。
  - 升级消耗选择器提示被消耗卡剩余体力不返还。
  - **顶部 Claw 标识仅激活期间显示**。
  - 7.2 叫法统一为"Claw sheet"；9.1 决策记录补阈值语义。
- v1.0 → v1.1（二轮体验评审修订）：
  - 图 1 一键补充逻辑更新为"内嵌购买补齐"（与 4.1 一致）。
  - **道具 sheet 只做查看库存/购买/Claw 激活**，移除"使用"跳转（避免底部抽屉+弹窗叠加）；库存行与道具名称**图标化**（桌面/移动端一致）。
  - Claw 三自动化默认值：自动补体力、0 体力自动休息**默认开**；自动最优演出**默认关**（用户手动开启）。
  - 一键补充确认时显示**预计总消耗**（"将消耗 N 个补给包"）。
  - 图 3 用户可见文案统一为「所有角色的每小时产出 +5%」。
  - 到期续费**固定推荐月卡**（可切换周卡）。
  - 4.3 获取途径去掉"商城"字样。
- v0.9 → v1.0（体验评审修订）：
  - **Claw 自动补体力阈值语义明确**：剩余工作时长"降至该值"时触发；默认 0 = 体力耗尽（=0）时才补；补给包不足时由「0 体力自动休息」接管。
  - **0 体力自动休息优先级明确**：体力=0 时，有包先补满；无包自动召回。
  - **一键补充支持内嵌购买**：库存不足时弹窗内嵌购买补齐缺口，或选择跳过。
  - 升级消耗的 2 张角色**由用户选择**（沿用现有升级选择交互）。
  - Claw 增加**购买后激活引导**（未激活角标）与**到期提醒**（剩≤3天变色、到期一键续费）。
  - Claw 用户可见文案统一为「所有角色的每小时产出 +5%」。
  - 新增交互原则：**移动端设计精简、文字精简**。
  - 用词清理：全文 studio/工作室 → 经纪人页（含图、落点、变更记录）。
- v0.8 → v0.9：
  - **范围收敛为纯道具**：移除 NFT 二级市场（挂单调度）、创作者激励（活动/故事发射奖励）相关内容；训练手册获取途径仅商城。
  - 背景描述去掉 Mint 提法。
  - 第 8 章联动表精简为：升级系统 / 体力工作时长 / 演出与硬顶。
- v0.7 → v0.8：
  - **升级条件去掉「累计完播门槛」**：升级 = 消耗 2 张同 IP 同等级角色 + 训练手册；升级消耗表移除门槛列（10,000/50,000/200,000/1,000,000）。
- v0.6 → v0.7：
  - 图 4 扩充为**内嵌购买三状态流转**（库存充足→直接补；库存不足→弹窗内展开购买面板→支付→原地刷新→补满）。
  - 明确内嵌购买规则：购买面板为弹窗内嵌二级视图（不关闭当前弹窗）、数量默认=缺口数、支付成功原地刷新并收起。
- v0.5 → v0.6：
  - 支付资产统一为 **USDC**（28 处 USDT 全部替换，与项目现有账本一致）。
  - 升级条件明确为「**消耗 2 张同 IP 同等级角色** + 完播门槛 + 训练手册」（名词统一为"角色"，沿用现有 2 张）。
  - Claw 自动补体力阈值：可调 **0~168h，默认 0**。
  - 内嵌购买：1 个起步 + 支付二次确认。
  - 补给包：任意状态角色均可补满。
  - +5% 不标注在名义产出处（避免 Claw 中途到期口径切换）；激活入口含顶部常驻标识。
  - 原型阶段支付流程**模拟**（demo，不做真实链上支付）。
  - 第 9 章改为「决策记录 + 剩余待定」；训练手册活动奖励发放规则归运营。
- v0.4 → v0.5：
  - 文案统一：全文档「质押」改为 页面场景用语「演出」（自动最优演出、结束演出召回、演出与硬顶等）。
  - 新增 1.5 交互示意图（4 张文字图：经纪人页入口地图 / 道具 sheet / Claw sheet / 内嵌购买）。
  - 全局规则新增 #7「背包即商城」：道具 sheet 内可直接购买并囤货。
  - 全局规则 #5 明确「价格以购买时为准，不追溯差价」。
- v0.3 → v0.4：
  - 确认**混合方案（方案 C）**落地：补给包/手册**上下文内嵌购买**；Claw 独立底部 sheet；经纪人页「更多」菜单新增「道具」入口（移动端不新增右上角按钮）。
  - 新增 1.4 交互设计原则（参考 StepN/Axie）：上下文内嵌购买、入口只减不增、卡片即入口、Claw 顶部常驻标识、一键补充联动补给包。
  - 「一键补充」批量操作改为消耗补给包补满（库存不足跳过并提示）。
  - 库存数量在**道具 sheet** 内查看（不放在角色卡角标）。
- v0.2 → v0.3：
  - 取消独立商城页与道具分类，4 个道具全部作为 **经纪人页** 功能落地（1.3、第 2 章、第 7 章）。
  - 明确体力补给包按 NFT 等级固定消耗；购买数量不设上限。
  - Story Claw 分周卡（7 天 / 800 STORY）与月卡（30 天 / 3000 STORY），效果一致，重复激活只延长有效期。
  - 删除「能量饮料」，其 +5% 效果并入 Story Claw（账号级、不改系数、受周硬顶约束、不叠加）。
  - 聚光灯、收藏道具、限时礼包：本期不做，归入后续版本。
