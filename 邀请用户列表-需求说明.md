# 邀请用户列表 — 需求说明文档

> **版本**: v1.0  
> **更新日期**: 2026-07-07  
> **关联页面**: `referral.html` — 邀请页面

---

## 1. 功能概述

在邀请页面上，用户点击「累计邀请人数」卡片，弹窗展示该用户邀请的**直接下级用户列表**，并明确标识**哪些是有效用户**。

---

## 2. 业务规则

### 2.1 数据范围

- 仅展示**直接下级用户**（一级邀请），不含间接邀请（Lv 2~5）。
- 直接下级用户定义：通过当前用户的邀请链接注册的用户。

### 2.2 有效用户的判定条件

满足以下**任一条件**即判定为有效用户：

1. **被邀请人实际挖到过 STORY** — 在 NFT 挖矿池中有过挖矿产出记录
2. **被邀请人发生过 USDC 付费** — 在平台上有过 USDC 支付行为（如购买 NFT、支付 Gas 费等）

仅注册但未做任何链上操作或付费行为的用户，视为**待激活用户**（无效用户）。

### 2.3 统计逻辑

弹窗顶部需展示三项统计数据：

| 统计项 | 计算方式 |
|--------|----------|
| 总人数 | 直接下级用户的总数 |
| 有效用户数 | 满足有效条件的用户数 |
| 待激活数 | 总人数 − 有效用户数 |

---

## 3. 功能逻辑

### 3.1 入口逻辑

- 「累计邀请人数」卡片可点击，点击后打开下级用户列表弹窗。
- 卡片上需有视觉提示（如箭头图标）表明可点击，避免用户误认为仅是一个展示数据块。

### 3.2 数据加载逻辑

- 页面加载时无需提前获取下级用户列表，仅展示累计人数。
- 用户点击卡片时触发数据请求（当前为 Mock 数据，后续对接后端 API）。
- 弹窗打开时显示加载状态（或直接显示内容）。
- 若数据加载失败，显示空状态提示。

### 3.3 列表展示逻辑

列表中的每一行应包含以下信息：

| 信息 | 说明 |
|------|------|
| 用户头像 | 帮助识别身份 |
| 用户名称 | 用户的显示名称 |
| 注册时间 | 格式：`YYYY-MM-DD`，不区分有效/无效都展示 |
| 有效状态标识 | 明确该用户是「有效用户」还是「待激活」 |

### 3.4 关闭逻辑

弹窗可通过以下方式关闭：

1. 点击弹窗右上角的 ✕ 关闭按钮
2. 点击弹窗外的遮罩区域

关闭后，弹窗数据无需缓存，下次打开重新加载。

### 3.5 边界情况

| 场景 | 处理方式 |
|------|----------|
| 用户未登录 | 弹窗不触发，引导登录 |
| 无下级用户 | 弹窗内显示空状态：`暂无直接下级用户` |
| 列表超长 | 弹窗限制最大高度，列表区域可滚动 |
| 头像加载失败 | 隐藏头像，不阻塞列表渲染 |

---

## 4. 数据接口定义

### 4.1 获取下级用户列表

**请求方式**: `GET /api/v1/invitees`

**请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `userId` | string | 是 | 当前登录用户 ID，用于查找其一级邀请关系 |
| `page` | int | 否 | 页码，默认 1 |
| `pageSize` | int | 否 | 每页条数，默认 20 |

**响应格式**:

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 6,
    "validCount": 4,
    "page": 1,
    "pageSize": 20,
    "list": [
      {
        "userId": "u_001",
        "name": "清风明月",
        "avatar": "https://cdn.example.com/avatar/001.jpg",
        "joinedAt": "2026-06-28T08:00:00Z",
        "isValid": true
      },
      {
        "userId": "u_002",
        "name": "星辰大海",
        "avatar": "https://cdn.example.com/avatar/002.jpg",
        "joinedAt": "2026-06-25T08:00:00Z",
        "isValid": true
      }
    ]
  }
}
```

### 4.2 有效状态判断（服务端）

服务端需根据仓库中的记录判断 `isValid`：

- 查询该用户是否有 `story_mining_records` 表中产出记录 > 0
- 或查询该用户是否有 `payment_records` 表中 USDC 付费记录
- 满足任一条件则 `isValid = true`

---

## 5. 前端 Mock 数据结构（实现参考）

```javascript
{
  name: '用户名',
  avatar: '头像URL',
  joined: '2026-06-28',     // 注册日期
  valid: true                // true=有效用户, false=待激活
}
```

---

## 6. 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-07-07 | 初始版本 |
