# Cline 行为规则

## 文件修改规则

- 对于 HTML/CSS/JS 等前端文件的简单修改，**优先使用 `replace_in_file` 工具直接修改**，禁止为简单的文本替换/样式调整编写 Python 脚本
- 避免执行 Python 脚本处理文件，因为频繁出现卡死情况
- 只有在以下场景才允许使用脚本：跨多文件复杂正则批量处理、需要解析结构化数据（如 JSON/YAML 深度处理）、需要调用外部 API 做数据处理

## 语言规则

- 使用中文 (zh-CN) 交流